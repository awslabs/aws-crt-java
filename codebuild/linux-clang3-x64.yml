version: 0.2
#this buildspec assumes the ubuntu 14.04 trusty image
phases:
  install:
    commands:
      - sudo add-apt-repository ppa:openjdk-r/ppa
      - sudo apt-get update -y
      - sudo apt-get install clang-3.9 cmake3 ninja-build libssl-dev openjdk-8-jdk-headless maven -y
  pre_build:
    commands:
      - export CC=clang-3.9
  build:
    commands:
      - echo Build started on `date`
      - $CODEBUILD_SRC_DIR/codebuild/common-linux.sh -DCMAKE_EXPORT_COMPILE_COMMANDS=ON
  post_build:
    commands:
      - echo Build completed on `date`

