version: 0.2

env:
  variables:
    AWS_CMAKE_GENERATOR: Visual Studio 15 2017 Win64

phases:
  build:
    commands:
      - npm install --global windows-build-tools@4.0.0
      - .\codebuild\mqtt5-tests-windows.bat
